#ifndef ${CPP_NAMESPACE}_version_h
#define ${CPP_NAMESPACE}_version_h

#include <string>

namespace ${CPP_NAMESPACE}
{
    namespace version
    {
        uint32_t major();
        uint32_t minor();
        uint32_t patch();
        uint32_t tweak();
        std::string str();
    }  // namespace version
}  // namespace ${CPP_NAMESPACE}

#endif /* ${CPP_NAMESPACE}_version_h */
