#ifndef ${CPP_NAMESPACE}_version_h
#define ${CPP_NAMESPACE}_version_h

#include <cstdint>
#include <string_view>

namespace ${CPP_NAMESPACE}
{
    namespace version
    {
    	inline constexpr uint32_t kMajor = ${MAJOR} + 0;
    	inline constexpr uint32_t kMinor = ${MINOR} + 0;
    	inline constexpr uint32_t kPatch = ${PATCH} + 0;
    	inline constexpr uint32_t kTweak = ${TWEAK} + 0;
    	inline constexpr std::string_view kStr = "${FULL_VERSION}";
    }  // namespace version
}  // namespace ${CPP_NAMESPACE}

#endif /* ${CPP_NAMESPACE}_version_h */
